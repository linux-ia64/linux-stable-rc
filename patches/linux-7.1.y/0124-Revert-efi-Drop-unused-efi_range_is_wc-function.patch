From 9eec8663f7f0b1e32d9732a8fbeb1c805c3d685e Mon Sep 17 00:00:00 2001
From: Johnny Mnemonic <jm@machine-hall.org>
Date: Mon, 20 Apr 2026 00:44:35 +0200
Subject: [PATCH 124/126] Revert "efi: Drop unused efi_range_is_wc() function"

ia64 is still using this in `arch/ia64/include/asm/fb.h`, so keeping that.

This reverts commit 77dd8adabbc8ff845177b460de48b9d2cd579966.
---
 include/linux/efi.h | 21 +++++++++++++++++++++
 1 file changed, 21 insertions(+)

diff --git a/include/linux/efi.h b/include/linux/efi.h
index 70a6efda6f12..80e87d442dd7 100644
--- a/include/linux/efi.h
+++ b/include/linux/efi.h
@@ -837,6 +837,27 @@ extern int __init parse_efi_signature_list(
 	const void *data, size_t size,
 	efi_element_handler_t (*get_handler_for_guid)(const efi_guid_t *));
 
+/**
+ * efi_range_is_wc - check the WC bit on an address range
+ * @start: starting kvirt address
+ * @len: length of range
+ *
+ * Consult the EFI memory map and make sure it's ok to set this range WC.
+ * Returns true or false.
+ */
+static inline int efi_range_is_wc(unsigned long start, unsigned long len)
+{
+	unsigned long i;
+
+	for (i = 0; i < len; i += (1UL << EFI_PAGE_SHIFT)) {
+		unsigned long paddr = __pa(start + i);
+		if (!(efi_mem_attributes(paddr) & EFI_MEMORY_WC))
+			return 0;
+	}
+	/* The range checked out */
+	return 1;
+}
+
 #ifdef CONFIG_EFI_PCDP
 extern int __init efi_setup_pcdp_console(char *);
 #endif
-- 
2.25.1

