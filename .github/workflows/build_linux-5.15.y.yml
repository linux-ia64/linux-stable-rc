name: 'signal_build_linux_5_15_y'
on:
  workflow_call:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: echo
        run: |
          echo "build linux-5.15.y"
